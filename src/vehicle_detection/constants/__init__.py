from pathlib import Path
## Path for configuration file
CONFIG_FILE_PATH= Path("configs/config.yaml")
## Path for params file which has all the project parameters
PARAMS_FILE_PATH= Path("params.yaml")
